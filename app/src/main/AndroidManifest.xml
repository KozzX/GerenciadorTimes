<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atapps.gerenciadortimes">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.TelaListaTime"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.TelaCadastroTime" />
        <activity
            android:name=".controller.TelaListaJogadores"
            android:label="@string/title_activity_tela_lista_jogadores"
            android:theme="@style/AppTheme" />
        <activity android:name=".controller.TelaCadastroJogador" />
        <activity
            android:name=".controller.SorteioTime"
            android:label="@string/title_activity_sorteio_time"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
